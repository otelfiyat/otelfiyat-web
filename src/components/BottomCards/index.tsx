import FeaturesCards from "./FeaturesCards";

const BottomCards = () => {
  return (
    <section className="bg-brand-white my-10">
      <div className="container mx-auto flex flex-col gap-4">
        <FeaturesCards />
      </div>
    </section>
  );
};

export default BottomCards;
